let ctx,gain,muted=false;export function initAudio(){if(ctx)return;ctx=new AudioContext();gain=ctx.createGain();gain.gain.value=Number(localStorage.getItem("shelf-volume")??.35);gain.connect(ctx.destination)}export function setVolume(v){localStorage.setItem("shelf-volume",v);if(gain)gain.gain.value=muted?0:v}export function toggleMute(){muted=!muted;if(gain)gain.gain.value=muted?0:Number(localStorage.getItem("shelf-volume")??.35);return muted}export function blip(freq=440,dur=.08){if(!ctx)return;const o=ctx.createOscillator(),g=ctx.createGain();o.frequency.value=freq;o.type="sine";g.gain.setValueAtTime(.0001,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.06,ctx.currentTime+.01);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+dur);o.connect(g).connect(gain);o.start();o.stop(ctx.currentTime+dur+.01)}